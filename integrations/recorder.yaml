recorder:
  purge_keep_days: 14

  exclude:
    domains:
      - device_tracker
      # - media_player
      - uptime
      - time_date
      - worldclock

    entities:
      - sensor.auto_backup
      - sensor.home_assistant_v2_db_size
